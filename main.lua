-- // main.lua - Entry point for the UI system

-- // Xenon, this is like, the main gateway to our awesome UI! totes! <3

local function init()
  print("ui system initializing...")
  -- // Later, we'll load the example from here
  -- // local ideLayout = loadstring(readfile("examples/IDELayout.lua"))()
  -- // if ideLayout and ideLayout.mount then
  -- //   ideLayout:mount()
  -- // end
end

init();